

## Replace Header Logo

Replace the current logo asset (`src/assets/offlineresume.png`) with the newly uploaded logo file.

### Changes

**1. Asset replacement**
- Copy `user-uploads://offlineresume_logo.png` to `src/assets/offlineresume.png`, overwriting the existing file

No code changes needed — the existing import in `LandingHeader.tsx` already references this asset path.

